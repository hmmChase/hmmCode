const perms = nums => {
  const result = [];

  if (nums.length - 1 === 0) return 'asdf';

  for (let i = 0; i < array.length; i++) {
    for (let j = 0; j < array.length; j++) {
      if (num[i] !== nums[j]) result.push(nums[i]);
    }
  }
};

console.log(perms([0, 1]));
